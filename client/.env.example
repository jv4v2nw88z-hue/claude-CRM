# Leave empty in development — vite.config.ts proxies /api to the backend, which
# keeps the session cookie same-origin.
#
# In production set this to the deployed API origin, e.g.
# VITE_API_BASE_URL="https://api.midigitalexpansion.com"
VITE_API_BASE_URL=
