{
  "name": "midigitalexpansion-crm",
  "version": "1.0.0",
  "private": true,
  "description": "Internal CRM for MiDigitalExpansion — built to make service-ladder progression and MRR impossible to forget. Runs entirely on Cloudflare: Workers, D1, R2 and Cron Triggers.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "typecheck": "npm run typecheck --prefix server && npm run typecheck --prefix client",
    "migrate:new": "npm run migrate:new --prefix server --",
    "migrate:local": "npm run migrate:local --prefix server",
    "migrate:remote": "npm run migrate:remote --prefix server",
    "seed:local": "npm run seed:local --prefix server",
    "seed:remote": "npm run seed:remote --prefix server",
    "deploy": "npm run build --prefix client && npm run deploy --prefix server",
    "qa:lifecycle": "npm run qa:lifecycle --prefix server"
  }
}
