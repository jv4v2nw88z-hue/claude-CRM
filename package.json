{
  "name": "midigitalexpansion-crm",
  "version": "1.0.0",
  "private": true,
  "description": "Internal CRM for MiDigitalExpansion — built to make service-ladder progression and MRR impossible to forget.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "typecheck": "npm run typecheck --prefix server && npm run typecheck --prefix client",
    "migrate": "npm run prisma:migrate --prefix server",
    "seed": "npm run seed --prefix server",
    "qa:lifecycle": "npm run qa:lifecycle --prefix server"
  }
}
